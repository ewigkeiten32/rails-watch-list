
<%= simple_form_for @bookmark do |f| %>
  <%= f.input :comment %>
  <%= f.association :movies %>
  <%= f.association :lists %>
  <br>
  <%= f.submit "Submit bookmark", class: "btn btn-primary" %>
<% end %>
