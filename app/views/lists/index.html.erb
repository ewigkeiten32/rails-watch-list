<h1>Here are your lists</h1>
<h3>
  <ul>
    <% @lists.each do |list| %>
    <li> <%= link_to list.name, list_path(list) %> || Bookmarks:
      <%= list.bookmarks.count %></li>
    <hr>
<% end %>
  </ul>
</h3>
<h2><%= link_to "Add New List", new_list_path, class: "btn btn-warning" %></h2>
<hr>
